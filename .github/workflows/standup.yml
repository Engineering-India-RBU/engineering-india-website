name: Engineering India Daily Standup

on:
  schedule:
    # Daily at 7 PM IST
    - cron: '30 13 * * *'

  workflow_dispatch:

jobs:
  send-standup:
    runs-on: ubuntu-latest

    steps:
    - name: Check Secret
      env:
        DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
      run: |
        echo "Secret length: ${#DISCORD_WEBHOOK_URL}"